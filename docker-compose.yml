services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: portfolio-website
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
